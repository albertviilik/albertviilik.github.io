import * as React from 'react';
import './dock.css'

type Props = {

};
type State = {
    selected: number
};

export class Dock extends React.Component<Props, State> {
    render() {
        return (
            <div className={"Dock"}>

            </div>
        );
    };
};
